ui/                         # User interface frontend/back-end code
├── __init__.py             # Python package initializer
├── main_content.py         # Main content area logic (loads courses, notes, media)
├── sidebar.py              # Sidebar logic (navigation, filters, menu)
├── notes_browser.py        # Notes browsing interface, search, index integration
├── styles.css              # CSS styles for UI
├── __pycache__/            # Python compiled cache
├── ui_index.json           # Index of UI components and their status
└── UI_UPLOAD_RULES.md      # Rules for uploading and naming files via the UI
